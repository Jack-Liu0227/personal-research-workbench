# Personal Research Workbench 0.0.5

Windows 10/11 x64 NSIS release for the research intelligence, RSS and Feishu delivery slice, together with the current Agent/runtime and workspace-service changes in this release tree.

## Highlights

- Added Intel Daily configuration and overview surfaces for research intelligence workflows.
- Added RSS source, category, preview, refresh and item-query flows with database persistence and workspace-service orchestration.
- Added Main-owned Feishu app binding, OAuth handoff/status, test-message delivery and credential-boundary handling.
- Added daily intelligence and RSS delivery contracts, migrations, repositories, MCP/service dispatch and focused tests.
- Improved Agent/Core/Main/Preload integration, research settings, ledger grouping and Electron E2E coverage.
- Added the Agent LLM integration skill and implementation guidance without exposing credentials or personal provider configuration.

## Release safety

- External writes and real provider/OAuth/Feishu/Zotero/Obsidian round-trips remain environment-dependent and are not implied by this release.
- Authenticode signing and packaged-app page-by-page UI regression remain open unless separately evidenced below.
- The release is a normal increment after the published `v0.0.4`; no downgrade is involved.

## Installer identity

- Installer: `Personal-Research-Workbench-0.0.5-Setup.exe`
- Size: `140,554,691` bytes
- SHA-256: `48ED5B57FF71426683BC48F4FC5B1FDD1064B827AC59E5601E8BA511915B6A94`
- ProductVersion: `0.0.5`
- Authenticode: unsigned (`NotSigned`)
- Blockmap: `Personal-Research-Workbench-0.0.5-Setup.exe.blockmap` (`149,275` bytes)
- Build path: default electron-builder path; the `.asar` fallback was not needed.

## Verification

- `node .agents/skills/windows-release/scripts/release.mjs version 0.0.5` PASS; all 11 package versions and the MCP identity report `0.0.5`.
- `node .agents/skills/windows-release/scripts/release.mjs build` PASS (`pnpm typecheck`, `pnpm build`, x64 NSIS).
- `node .agents/skills/windows-release/scripts/release.mjs verify` PASS for installer identity and packaged payload.
- `node .agents/skills/windows-release/scripts/release.mjs smoke` PASS: silent install, isolated `--prw-user-data-dir`, first launch, SQLite header, 37 migrations, 4 seeded schedules, empty user tables, process stop and silent uninstall exit code 0. The smoke profile was cleared; the install directory retained 9 Windows/asar-lock-related entries and the uninstall shortcut/entry remained, as reported by the script.
- `pnpm test:rss-daily-msg` PASS (54 tests), `pnpm test:feishu-binding` PASS (9 tests), `pnpm test:intel-daily` PASS (8 tests).
- `pnpm test:e2e` PASS: shell, navigation, RSS intelligence source filter, Agent, settings, project-space, literature, Zotero, Obsidian, Feishu binding card, RSS source card, schedules and responsive/overflow checks.
- `node scripts/capture-readme-shots.cjs` PASS: regenerated the 10 existing screenshots and added `docs/assets/screenshots/11-rss.png` using an isolated profile and local synthetic RSS feed; no external or personal feed data was captured.

The NSIS smoke still reports 9 transient Windows/asar-lock-related install entries after uninstall; the isolated profile is cleared and shortcuts/uninstall registration are removed. Known remaining environment-dependent checks: real OAuth/browser callback, real Feishu delivery, disposable Zotero/Obsidian external write round-trips, code signing and packaged-app page-by-page UI regression. Auto-update wiring remains outside this release scope.
